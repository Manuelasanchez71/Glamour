export interface IUser {
  idUsuario?: number; // Identificador opcional del usuario
  nombre: string;     // Nombre del usuario
  correo: string;     // Correo electrónico del usuario
  password: string;   // Contraseña del usuario
}
